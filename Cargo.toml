[package]
name = "file-organizer"
version = "0.1.0"
edition = "2021"
description = "A CLI tool to organize files by their extensions into categorized folders"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
libc = "0.2"

[dev-dependencies]
tempfile = "3.10"
